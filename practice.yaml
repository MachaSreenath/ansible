- name: install packages
  hosts: web
  become: yes
  tasks: 
  - name: installation of packages
    ansible.builtin.package:
      msg: "hello {{item}}"
    loop:
    - git
    - postfix
    - mysql
