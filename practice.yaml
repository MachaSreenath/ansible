- name: variables
  hosts: localhost
  vars:
    name: John
    wish: Morning
    awish: niceday

  tasks:
  - name: variable testing
    vars: 
      name: ibrahim
    ansible.builtin.debug:
      msg: "Hello, {{ name }}, {{wish}}, have a {{awish}}"